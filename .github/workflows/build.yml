name: Build APK
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Buildozer Action
        uses: Arden99/Buildozer-Action@v4.0
        with:
          repository_txt: ""
